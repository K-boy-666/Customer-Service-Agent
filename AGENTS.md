# AGENTS.md - Customer Service Agent 2.0

> Auto-generated by harness-creator. Updated 2026-06-26. See `feature_list.json` for current state.

## Project Identity

- **Name**: Customer Service Agent 2.0
- **Stack**: Python 3.10+, FastMCP, SQLAlchemy, SQLite (dev) / MySQL (prod target)
- **Entry point**: `customer-service-orchestrator` - the only customer-facing agent
- **Repository**: github.com/K-boy-666/Customer-Service-Agent

## Architecture At A Glance

```text
Customer -> Orchestrator -> Dispatcher (intent engine)
                         -> order-inquiry-agent (L0)
                         -> consultation-agent (L0)
                         -> after-sales-agent (L1)
                         -> work-order-agent (L1)
                         -> complaint-agent (L2)
                         -> human-handoff-agent (L2)
```

All customer routing goes through the Orchestrator. Sub-agents never face customers directly.

## How To Start

Windows command prompt or PowerShell:

```powershell
.\init.cmd --check-only --skip-tests
.\init.cmd
```

POSIX shell:

```bash
bash init.sh --check-only --skip-tests
bash init.sh
```

If running the REST API separately:

```bash
uvicorn order_api:app --reload --port 8000
```

## Working Rules

1. **Always route through the Orchestrator.** Never dispatch to sub-agents directly from a customer conversation.
2. **Run the init check at session start.** Use `./init.cmd --check-only --skip-tests` on Windows or `bash init.sh --check-only --skip-tests` where Bash is available.
3. **Read `progress.md`** to find active work and blockers.
4. **Check `feature_list.json`** for feature dependencies before starting new work.
5. **Append a `## Session` block to `session-handoff.md`** before ending every session.
6. **Test before claiming done:** run `uv run pytest tests/ -q`.
7. **Respect permission tiers:** L0 agents are read-only, L1 can create records, L2 is conversation-only.
8. **Follow ADR-0002 protocol:** input uses customer context + task; output uses processing result + customer reply + internal notes.

## Fast Agent Workflow

- Prefer `C:\Users\JOSAP\scoop\shims\rg.exe` for searches. In an existing PowerShell session, prepend `$env:USERPROFILE\scoop\shims` to `PATH` before running `rg`.
- Do not run unrestricted `Get-ChildItem -Recurse` from the repository root. If PowerShell fallback search is needed, exclude `.venv`, `.git`, `.pytest_cache`, and `__pycache__`.
- When testing Chinese customer messages in PowerShell, use UTF-8 file input or Unicode escapes. Avoid inline Chinese here-strings that can be corrupted by the console code page.
- When diagnosing customer requests, reuse MCP or the same Python process where practical. Avoid one cold Python/FastMCP startup per message.
- For one-off customer-message diagnostics, call `orchestrator_mcp_tool.handle_customer_message_tool` directly, or reuse an already-running MCP server. Import `server_customer.py` only when testing or running the full MCP server.

## Definition Of Done

A feature is done only when all of these are true:

- Feature is listed in `feature_list.json` with status `done` and done criteria satisfied.
- Tests pass: `uv run pytest tests/ -q` returns 0.
- Verification evidence is recorded in `progress.md`.
- `./init.cmd` or `bash init.sh` completes all 6 stages with no failures.
- `node scripts/harness/validate-harness.mjs` reports no new warnings.
- A session handoff block is appended to `session-handoff.md`.

## Harness Subsystem Map

| Subsystem | Artifact | Health |
|-----------|----------|--------|
| Instructions | `CLAUDE.md`, `AGENTS.md` | OK |
| State | `feature_list.json`, `progress.md` | OK |
| Verification | `init.sh`, `init.cmd`, `init.ps1`, `scripts/harness/init_check.py`, `scripts/harness/validate-harness.mjs` | OK |
| Scope | Feature dependencies in `feature_list.json` | OK |
| Lifecycle | `session-handoff.md` | OK |

## Key Paths

| Path | Purpose |
|------|---------|
| `CLAUDE.md` | Primary instruction file |
| `AGENTS.md` | Onboarding and conventions |
| `feature_list.json` | Feature inventory with dependencies and done criteria |
| `progress.md` | Active work, recent completions, blockers, verification state |
| `init.sh` | POSIX wrapper for cross-platform init checks |
| `init.cmd` | Windows command wrapper for cross-platform init checks |
| `scripts/harness/init_check.py` | 6-stage verification implementation |
| `session-handoff.md` | Cross-session context transfer |
| `scripts/harness/` | Node.js automation and harness validators |
| `docs/adr/` | Architecture Decision Records |
| `.claude/agents/` | Agent system prompts |
| `.claude/mcp.json` | MCP server launch config |
| `src/log_config.py` | structlog configuration with stdlib logging bridge |
| `src/rate_limit.py` | slowapi rate limiter setup and LIMIT_* constants |
| `src/request_logging.py` | Pure ASGI request logging middleware (contextvars binding) |
| `tests/` | pytest suite |

## External Services

| Service | Command | Port |
|---------|---------|------|
| Order REST API | `uvicorn order_api:app --reload --port 8000` | 8000 |
| customer-service MCP | Auto-launched via `.claude/mcp.json`; only customer-facing MCP entry point | stdio |
| order-server MCP | Auto-launched via `.claude/mcp.json`; internal read-only support only | stdio |
